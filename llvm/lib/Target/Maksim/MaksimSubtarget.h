#ifndef LLVM_LIB_TARGET_MAKSIM_MAKSIMSUBTARGET_H
#define LLVM_LIB_TARGET_MAKSIM_MAKSIMSUBTARGET_H

#include "Maksim.h"
#include "MaksimFrameLowering.h"
#include "MaksimISelLowering.h"
#include "MaksimInstrInfo.h"
#include "MaksimRegisterInfo.h"
#include "llvm/CodeGen/SelectionDAGTargetInfo.h"
#include "llvm/CodeGen/TargetSubtargetInfo.h"

#define GET_SUBTARGETINFO_HEADER
#include "MaksimGenSubtargetInfo.inc"

namespace llvm {

class MaksimSubtarget : public MaksimGenSubtargetInfo {
  MaksimTargetLowering TLInfo;
  MaksimFrameLowering FrameLowering;
  MaksimRegisterInfo RegInfo;
  MaksimInstrInfo InstrInfo;
  SelectionDAGTargetInfo TSInfo;

public:
  MaksimSubtarget(const Triple &TT, const std::string &CPU, const std::string &FS,
               const TargetMachine &TM);

  /// ParseSubtargetFeatures - Parses features string setting specified
  /// subtarget options.  Definition of function is auto generated by tblgen.
  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);

  const MaksimTargetLowering *getTargetLowering() const override {
    MAKSIM_DUMP_CYAN
    return &TLInfo;
  }
  const MaksimFrameLowering *getFrameLowering() const override {
    MAKSIM_DUMP_CYAN
    return &FrameLowering;
  }
  const MaksimRegisterInfo *getRegisterInfo() const override {
    MAKSIM_DUMP_CYAN
    return &RegInfo;
  }
  const MaksimInstrInfo *getInstrInfo() const override { return &InstrInfo; }
  const SelectionDAGTargetInfo *getSelectionDAGInfo() const override {
    MAKSIM_DUMP_CYAN
    return &TSInfo;
  }
};

} // end namespace llvm

#endif // LLVM_LIB_TARGET_MAKSIM_MAKSIMSUBTARGET_H